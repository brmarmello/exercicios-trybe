function sum(number1, number2) {
  return number1 + number2;
}

// console.log(
//   'O resultado do calculo 2 + 7 = ' + sum(2, 7) + '.\nObrigado pela atenção!',
// );

console.log(
  `O resultado do calculo 2 + 7 = ${sum(2, 7)}.
Obrigado pela atenção!`,
);

const sumResult = sum(2, 7);
console.log(
  `O resultado do calculo 2 + 7 = ${sumResult}. Obrigado pela atenção!`,
);
